---
interface Props {
  class?: string;
}
const { class: className, ...rest } = Astro.props;
---

<button
  class=`h-10 px-6 w-fit font-medium text-white bg-green-500 rounded-full transition hover:opacity-50 bg-gradient ${className}`
>
  <slot /></button
>
